include ../layouts/head
include ../layouts/nav

doctype html
html(lang="en")
  +head('Home')
  body
    +nav('home')

    main
      section(id="home")
        .home-wrapper.wrapper
          article.intro-text
            h1
              span.h5 So, You want to travel to
              br
              span.space.h1 Space
            p.txt Let’s face it; if you want to go to space, you might as well genuinely go to outer space and not hover kind of on the edge of it. Well sit back, and relax because we’ll give you a truly out of this world experience!
          
          a.explore.center-flex(href="destination.html", onclick="event.preventDefault();console.log(document.documentElement.offsetHeight)") EXPLORE

    script
      include ../../js/dist/nav.js